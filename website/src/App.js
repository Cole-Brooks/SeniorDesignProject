import React from 'react';
import './App.css';

class App extends React.Component {
  render() {
    return (
        <div className="container py-5 text-center">
          <div className="py-5">
            <div className="py-5">
              <h1 className="class py-5 text-primary">Big Welcome Team Cash!</h1>
              <h2 className="py-5">The backend server(Django) is up and running.</h2>
              <h3>React app(web) is set up and this is being served using React. Still needs to set up
                mobile(app) with React-Native. So the codes can be shared between different platforms</h3>
            </div>
          </div>
    </div>
  );
  }
}

export default App;
